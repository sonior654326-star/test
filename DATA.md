# DATA.md — 场景数据结构与规则

## 数据流

Demo 阶段：`data/scenes.ts`（静态种子，类型定义在 `lib/types.ts`）
生产阶段：Supabase（结构见 `supabase/schema.sql`，类型不变）

## 场景收录标准

一个影视场景值得收录，当且仅当它展示了**至少一种可迁移的人机关系机制**，
而不是因为"有 AI 出场"或"界面好看"。

优先覆盖的机制维度（每批数据都应保持机制多样性）：

语音交互 / 主动介入 / 长期记忆 / 情绪与关系 / 空间存在 / 机器人协作 /
权限与控制 / 多人交互 / 错误与人类接管 / 人格化表达 / 状态透明度 /
信任建立 / 目标对齐 / 隐私边界

## 14 维机制拆解（每条场景必填）

人的目标 / AI 的目标 / 触发条件 / AI 的动作 / 人的反应 / 反馈回路 /
情绪变化 / 信任变化 / 权限模型 / 记忆机制 / 失败模式 / 恢复方式 /
关系变化 / 环境角色

拆解质量标准：每一维写的是**机制**（可迁移到别的产品的规律），
不是**剧情复述**。检验方法：把作品名遮住，这句话对产品设计师还有没有用。

## 认知视角的写作规则

- **原始观察**（observationPrompts）：只提问，不给结论。好的观察问题
  指向一个具体瞬间（"注意她第一句话在建立什么"），而不是抽象讨论。
- **同理心视角**（empathy）：写场景中人的真实感受与未说出口的需求，
  最后一句必须落到对产品设计的含义。
- **产品启发**（productInspiration）：每条是一个命名的、可迁移的机制
  （如"判断标准汇报交互模式"），不是泛泛的建议。
- **可落地方向**（applicableProducts）：具体的产品品类，4 条左右。

## 版权规则

- 不存储、不托管任何影视文件；不外链盗版片段。
- 视频只允许以 YouTube 嵌入方式出现，且仅限官方渠道（片方/发行方/Movieclips 等
  持权渠道）或开放授权内容（CC/公有领域）；`video.source` 字段必须注明来源。
- `position` 字段只描述场景在原片中的位置，供用户在正版渠道自行查看。
- 截图暂不使用；如未来使用，仅限少量、带批注、评论性质。

## 当前数据量

11 个场景 / 10 部作品（第一批种子，见 data/scenes.ts）。
目标：第一个月内达到 50 条，机制维度全覆盖。
